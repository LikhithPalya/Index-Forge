spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/indexforge_test
    username: indexforge
    password: indexforge

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

  flyway:
    enabled: false